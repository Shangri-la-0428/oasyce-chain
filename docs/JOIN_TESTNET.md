# Join Oasyce Testnet

This document is kept only as a compatibility redirect.

The old join flow has been split into two cleaner entrypoints:

- For normal public testnet onboarding and direct chain usage, use [PUBLIC_BETA.md](PUBLIC_BETA.md).
- For validator or infrastructure setup, use [VALIDATOR_SETUP.md](VALIDATOR_SETUP.md).

If you only want the Docker helper script, you can still run:

```bash
bash scripts/join_testnet.sh
```

But the canonical onboarding and operator guidance now lives in the two documents above.
